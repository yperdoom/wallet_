import { Request, Response, NextFunction } from "express";
import Logger from "../../library/Logger";
const log = new Logger();

export function getMovement (request: Request, response: Response, _next: NextFunction) {
    
}

export function createMovement (request: Request, response: Response, _next: NextFunction) {

}

export function updateMovement (request: Request, response: Response, _next: NextFunction) {

}
