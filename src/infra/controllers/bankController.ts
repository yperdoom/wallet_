import { Request, Response, NextFunction } from "express";
import Logger from "../../library/Logger";
const log = new Logger();

export function getBank (request: Request, response: Response, _next: NextFunction) {
    
}

export function createBank (request: Request, response: Response, _next: NextFunction) {

}

export function updateBank (request: Request, response: Response, _next: NextFunction) {

}
