import { Request, Response, NextFunction } from "express";
import Logger from "../../library/Logger";
const log = new Logger();

export async function getBank (request: Request, response: Response, _next: NextFunction) {
    
}

export async function createBank (request: Request, response: Response, _next: NextFunction) {

}

export async function updateBank (request: Request, response: Response, _next: NextFunction) {

}
