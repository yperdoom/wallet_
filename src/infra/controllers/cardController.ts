import { Request, Response, NextFunction } from "express";
import Logger from "../../library/Logger";
const log = new Logger();

export async function getCard (request: Request, response: Response, _next: NextFunction) {
    
}

export async function createCard (request: Request, response: Response, _next: NextFunction) {

}

export async function updateCard (request: Request, response: Response, _next: NextFunction) {

}
