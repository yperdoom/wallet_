import { Request, Response, NextFunction } from "express";
import Logger from "../../library/Logger";
const log = new Logger();

export function getCard (request: Request, response: Response, _next: NextFunction) {
    
}

export function createCard (request: Request, response: Response, _next: NextFunction) {

}

export function updateCard (request: Request, response: Response, _next: NextFunction) {

}
